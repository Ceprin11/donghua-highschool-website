# better-sqlite3 13 bundles native prebuilds for supported platforms.
# Avoid npm's implicit node-gyp fallback for its binding.gyp; no lifecycle
# installer is required by this project's pinned runtime dependencies.
ignore-scripts=true
