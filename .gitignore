#env
.env
.env.*

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
/dist/
/dist-ssr/
*.local

# Nitro build output, once this app has a server. Both are regenerated by
# every build, and the deploy runs one before reading them.
.output
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vite
base44/.app.jsonc

# Local audit reports and export metadata are not public source assets.
_project_review/
_export-report.json
!.env.example
.npm-cache/
.data/
/data/
backups/
test-results/
playwright-report/
.browser-cache/

# Third-party source mirrors are kept locally. Deployments use the prepared
# browser assets under public/experiments.
/vendor/

# The 657 MB GPT-2 shard set is persisted on the review server. The deploy
# workflow restores it before building the container.
/public/experiments/transformer/model-v2/
